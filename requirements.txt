fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
jinja2>=3.1
httpx>=0.27
python-docx>=1.1
reportlab>=4.2
# Phase 14: PDF text extraction for ATS parse validation (never used to
# write PDFs -- reportlab above remains the only writer).
pypdf>=5.0
python-multipart>=0.0.9
pytest>=8.3

# Phase 6: PostgreSQL backend driver. Only imported/used when DATABASE_URL
# points at postgres:// -- SQLite-only local installs never touch this
# module at runtime, but it must be installed for the postgres:// code path
# (app/db_postgres.py) to be importable at all. See docs/postgres-backend.md.
psycopg[binary]>=3.1
